#' @param from vector of distinct integers. It is recycled to the length of `to` if it has length 1. Otherwise, it must have the same length as `to`.
#'
#' @param to see `from` for details. Defaults to `+Inf`.
