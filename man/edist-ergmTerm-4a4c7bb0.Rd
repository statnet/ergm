% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InitErgmTerm.R
\name{edist-ergmTerm}
\alias{edist-ergmTerm}
\alias{InitErgmTerm.edist}
\title{Euclidean distance between nodes on numeric coordinates}
\usage{
# binary: edist(attr,
#               pow=1)
}
\arguments{
\item{attr}{a vertex attribute specification (see Specifying Vertex attributes and Levels (\code{?nodal_attributes}) for details.)}

\item{pow}{the power (or vector of powers) to which the Euclidean distance is
raised. \code{pow=1} (the default) makes the log-odds of a tie linear in
Euclidean distance; \code{pow=2} gives squared Euclidean distance (a
Gaussian-type decay in tie probability), and larger powers produce sharper
declines. Supplying a vector of powers adds one statistic per power, so a
single term can capture a polynomial in distance (e.g., \code{pow=c(1,2)}).}
}
\description{
This term adds one network statistic for each element of \code{pow},
equal to the sum over all edges \verb{(i,j)} of the Euclidean distance between
nodes \code{i} and \code{j} in the space defined by two or more numeric nodal
attributes (e.g., latitude and longitude), raised to the corresponding
power.
}
\details{
For an edge between nodes \eqn{i} and \eqn{j} with numeric
coordinates \eqn{x_i} and \eqn{x_j} (each a vector over the supplied
\code{attr}), the per-edge statistic is the Euclidean distance
\eqn{d_{ij} = \sqrt{\sum_k (x_{ik} - x_{jk})^2}} raised to the power \code{pow}.
At least two numeric attributes are required; use \code{\link[=absdiff-ergmTerm]{absdiff}}
for a single dimension.

Unlike an equivalent \code{\link[=edgecov-ergmTerm]{edgecov}} specification, \code{edist}
computes the distance on the fly from the length-\eqn{n} coordinate vectors
and never materializes the \eqn{n \times n} distance matrix, so its memory
cost is linear rather than quadratic in network size.
}
\seealso{
\code{\link{ergmTerm}} for index of model terms currently visible to the package.

\Sexpr[results=rd,stage=render]{ergm:::.formatTermKeywords("ergmTerm", "edist", "subsection")}
}
\concept{directed}
\concept{dyad-independent}
\concept{quantitative nodal attribute}
\concept{undirected}
